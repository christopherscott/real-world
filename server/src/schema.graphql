# import Post from "./generated/prisma.graphql"

type Query {
  posts: [Post!]!
  post(id: ID!): Post
  search(search: String!): [Post!]!
}

type Mutation {
  createPost(title: String!, text: String): Post
  deletePost(id: ID!): Post
}
